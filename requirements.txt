# Core
ultralytics>=8.3.0
torch>=2.2.0
torchvision>=0.17.0

# Vision / preprocessing
opencv-python>=4.9.0
numpy>=1.24.0
PyWavelets>=1.5.0
albumentations>=1.4.0
Pillow>=10.0.0

# Data / utils
pyyaml>=6.0
tqdm>=4.66.0
requests>=2.31.0
gdown>=5.1.0
pandas>=2.0.0

# Evaluation / viz
matplotlib>=3.8.0

# Deployment (optional; install as needed for your platform)
onnx>=1.16.0
onnxruntime-gpu>=1.18.0
# tensorrt           # NVIDIA 환경에 맞춰 별도 설치 권장

# Tracking
# BoT-SORT/ByteTrack는 ultralytics 내장 트래커 사용 (별도 설치 불필요)
